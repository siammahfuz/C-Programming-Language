#include<stdio.h>
int main()
{
	char ch = 'p';
	if(ch=='a')
	{
		printf("%c is lower case\n", ch);
	}
	else if(ch=='A')
	{
		printf("%c is upper  case\n", ch);
	}
	else if(ch=='a')
	{
		printf("%c is lower case\n", ch);
	}
	if(ch=='B')
	{
		printf("%c is upper case\n", ch);
	}
	if(ch=='b')
	{
		printf("%c is lower case\n", ch);
	}

	return 0;
}
